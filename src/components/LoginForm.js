import React, {useState, useRef} from 'react';
import {View, Text} from 'react-native';

const LoginForm = ({todos, navigation}) => {
  return (
    <View>
      <Text>LOGIN list</Text>
    </View>
  );
};

export default LoginForm;
